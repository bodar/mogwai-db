diff --git a/gremlin-js/gremlin-javascript/test/helper.js b/gremlin-js/gremlin-javascript/test/helper.js
index fab0452578..162ef652f8 100644
--- a/gremlin-js/gremlin-javascript/test/helper.js
+++ b/gremlin-js/gremlin-javascript/test/helper.js
@@ -24,14 +24,20 @@
 import DriverRemoteConnection from '../lib/driver/driver-remote-connection.js';
 import Client from '../lib/driver/client.js';
 
+// Ports are overridable so the suite can run against a server on a non-default port
+// (e.g. two suites on one CI host, or a port already taken locally). Defaults are
+// unchanged, so an environment that sets neither behaves exactly as before.
+const port = process.env.GREMLIN_SERVER_PORT || '45940';
+const authPort = process.env.GREMLIN_SERVER_AUTH_PORT || '45941';
+
 let serverUrl;
 let serverAuthUrl;
 if (process.env.DOCKER_ENVIRONMENT === 'true') {
-  serverUrl = 'http://gremlin-server-test-js:45940/gremlin';
-  serverAuthUrl = 'https://gremlin-server-test-js:45941/gremlin';
+  serverUrl = `http://gremlin-server-test-js:${port}/gremlin`;
+  serverAuthUrl = `https://gremlin-server-test-js:${authPort}/gremlin`;
 } else {
-  serverUrl = 'http://localhost:45940/gremlin';
-  serverAuthUrl = 'https://localhost:45941/gremlin';
+  serverUrl = `http://localhost:${port}/gremlin`;
+  serverAuthUrl = `https://localhost:${authPort}/gremlin`;
 }
 
 export { serverUrl };
diff --git a/gremlin-js/gremlin-javascript/test/integration/traversal-test.js b/gremlin-js/gremlin-javascript/test/integration/traversal-test.js
index 66f0792f09..5fe65887f8 100644
--- a/gremlin-js/gremlin-javascript/test/integration/traversal-test.js
+++ b/gremlin-js/gremlin-javascript/test/integration/traversal-test.js
@@ -33,7 +33,7 @@ import {
 } from '../../lib/process/traversal-strategy.js';
 import GremlinLang from '../../lib/process/gremlin-lang.js';
 import DriverRemoteConnection from '../../lib/driver/driver-remote-connection.js';
-import { getConnection, getDriverRemoteConnection } from '../helper.js';
+import { getConnection, getDriverRemoteConnection, serverUrl } from '../helper.js';
 const __ = statics;
 
 let connection;
@@ -350,12 +350,6 @@ describe('Traversal', function () {
   });
 });
 
-let serverUrl;
-if (process.env.DOCKER_ENVIRONMENT === 'true') {
-  serverUrl = 'http://gremlin-server-test-js:45940/gremlin';
-} else {
-  serverUrl = 'http://localhost:45940/gremlin';
-}
 
 describe('CompositePDT - Traversal API', function () {
   describe('raw PDT round-trip via Traversal API', function () {
