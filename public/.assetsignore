# Keep git housekeeping files out of the uploaded Static Assets set (gitignore syntax; wrangler
# excludes this file itself). Only scalar.js — the docs UI served at /scalar.js — should ship.
.gitkeep
.gitignore
